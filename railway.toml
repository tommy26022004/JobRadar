[build]
builder = "dockerfile"
dockerfilePath = "jobradar-backend/Dockerfile"
buildContext = "jobradar-backend"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
